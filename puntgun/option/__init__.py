"""
Classes that sharing same name with configuration options.

They inherit configuration parsing base classes to
initialing themselves by setting configured values as attributes.

And they inherit judgement processing base classes to
request and judge target users' information with configured attributes
in a reactive (stream-like) way.
"""
